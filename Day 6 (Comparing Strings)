#comparing two strings taken by user for anagrams
str1 = input("Enter the first word : ")
str2 = input("Enter the second word : ")

str1 = list(str1.upper())
str2 = list(str2.upper())
str1.sort(),str2.sort()

if(str1==str2):
    print("Equal")
    
else:
    print("Not equal")
#coded by khushi
